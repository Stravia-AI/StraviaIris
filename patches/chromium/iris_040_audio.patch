目的：真实离线与实时音频图统一采用 profile 采样率，保留原生输入校验和信号处理，不施加音频扰动。
来源：原创 StraviaIris；CEF https://github.com/chromiumembedded/cef/tree/708dc140cbc3286826a8abef89dc23a44ff9ea72
基线：https://chromium.googlesource.com/chromium/src/+/79460ebecaa5625e57a5fb679a735659e73dc687
许可：新增组件 BSD-3-Clause；Chromium、CEF、ANGLE 修改保留各自原有许可。
输入：固定提交与 Windows profile；不改宿主原型、不注入伪装脚本、不改变 CEF 公共 API hash。
验收要求：离线和实时上下文实际采样率、非法输入拒绝、原生 analyser 输出与短数组语义。

diff --git third_party/blink/renderer/modules/webaudio/BUILD.gn third_party/blink/renderer/modules/webaudio/BUILD.gn
index 723759fed653ec09bd4c00a49fe1b6f326ee7d83..1c75be25633a7f7934753f3a60b98fd5bbddb889 100644
--- third_party/blink/renderer/modules/webaudio/BUILD.gn
+++ third_party/blink/renderer/modules/webaudio/BUILD.gn
@@ -188,6 +188,7 @@ blink_modules_sources("webaudio") {
     sources += [ "cpu/arm/oscillator_kernel_neon.cc" ]
   }
   deps = [
+    "//components/iris",
     "//services/metrics/public/cpp:ukm_builders",
     "//third_party/blink/renderer/modules/mediastream",
     "//third_party/blink/renderer/modules/peerconnection",
diff --git third_party/blink/renderer/modules/webaudio/audio_context.cc third_party/blink/renderer/modules/webaudio/audio_context.cc
index 61cc6314ce06cefd52c6bdbe739e469b4995eb14..71beb65c2c17602e51bda3fe4dfc9eaa18472551 100644
--- third_party/blink/renderer/modules/webaudio/audio_context.cc
+++ third_party/blink/renderer/modules/webaudio/audio_context.cc
@@ -14,6 +14,7 @@
 #include "base/strings/to_string.h"
 #include "base/trace_event/trace_event.h"
 #include "build/build_config.h"
+#include "components/iris/profile_data.h"
 #include "media/audio/audio_device_description.h"
 #include "media/base/output_device_info.h"
 #include "services/metrics/public/cpp/ukm_builders.h"
@@ -509,6 +510,10 @@ AudioContext* AudioContext::Create(ExecutionContext* context,
     return nullptr;
   }
 
+  // Validate the caller's rate first; the profile rate drives the actual graph,
+  // including render-quantum validation and destination resampling.
+  sample_rate = static_cast<float>(iris::profile::kAudioSampleRate);
+
   // Pre-validation for renderSizeHint if sampleRate is provided. This prevents
   // allocating excessive memory for clearly invalid renderSizeHint values
   // before the AudioContext is fully constructed.
diff --git third_party/blink/renderer/modules/webaudio/offline_audio_context.cc third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
index aed92e0ec46ada61b10cc7d7afaa1e20b08b7e36..dcadcec2bd337d101a9b7b1dc9bccb4f78883044 100644
--- third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
+++ third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
@@ -37,6 +37,7 @@
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/modules/webaudio/audio_listener.h"
 #include "third_party/blink/renderer/modules/webaudio/deferred_task_handler.h"
+#include "components/iris/profile_data.h"
 #include "third_party/blink/renderer/modules/webaudio/offline_audio_completion_event.h"
 #include "third_party/blink/renderer/modules/webaudio/offline_audio_destination_node.h"
 #include "third_party/blink/renderer/platform/audio/audio_utilities.h"
@@ -120,6 +121,11 @@ OfflineAudioContext* CreateOfflineAudioContext(
     return nullptr;
   }
 
+  // Iris: 上下文与渲染图固定在 profile 采样率；调用方请求的采样率只用于
+  // 渲染结果 AudioBuffer 的对外报告值（与参考引擎的设备速率行为一致）。
+  const float requested_sample_rate = sample_rate;
+  sample_rate = static_cast<float>(iris::profile::kAudioSampleRate);
+
   SCOPED_UMA_HISTOGRAM_TIMER("WebAudio.OfflineAudioContext.CreateTime");
   OfflineAudioContext* audio_context =
       MakeGarbageCollected<OfflineAudioContext>(
@@ -133,6 +139,7 @@ OfflineAudioContext* CreateOfflineAudioContext(
     return nullptr;
   }
 
+  audio_context->SetBufferReportedSampleRate(requested_sample_rate);
   audio_context->UpdateStateIfNeeded();
 
 #if DEBUG_AUDIONODE_REFERENCES
@@ -241,8 +248,11 @@ ScriptPromise<AudioBuffer> OfflineAudioContext::startOfflineRendering(
   complete_resolver_ = MakeGarbageCollected<ScriptPromiseResolver<AudioBuffer>>(
       script_state, exception_state.GetContext());
 
-  // Allocate the AudioBuffer to hold the rendered result.
-  float sample_rate = DestinationHandler().SampleRate();
+  // Allocate the AudioBuffer to hold the rendered result. Iris: 缓冲区对外
+  // 报告调用方请求的采样率，而渲染图在 profile 采样率上运行。
+  const float sample_rate = BufferReportedSampleRate() > 0
+                                ? BufferReportedSampleRate()
+                                : DestinationHandler().SampleRate();
   unsigned number_of_channels = DestinationHandler().NumberOfChannels();
 
   AudioBuffer* render_target = AudioBuffer::CreateUninitialized(
diff --git third_party/blink/renderer/modules/webaudio/offline_audio_context.h third_party/blink/renderer/modules/webaudio/offline_audio_context.h
index 48a8fa86e550e30b88d44fe2bd4302169a357d13..2787a4cd7cc9fec59a08b05f454bb1ec65cb5bae 100644
--- third_party/blink/renderer/modules/webaudio/offline_audio_context.h
+++ third_party/blink/renderer/modules/webaudio/offline_audio_context.h
@@ -110,6 +110,15 @@ class MODULES_EXPORT OfflineAudioContext final : public BaseAudioContext {
 
   bool HasPendingActivity() const final;
 
+  // Iris: 渲染缓冲区对外报告调用方请求的采样率，而上下文与渲染图固定在
+  // profile 采样率（与参考引擎的设备速率行为一致）。
+  void SetBufferReportedSampleRate(float rate) {
+    buffer_reported_sample_rate_ = rate;
+  }
+  float BufferReportedSampleRate() const {
+    return buffer_reported_sample_rate_;
+  }
+
  private:
   // Fetch directly the destination handler.
   OfflineAudioDestinationHandler& DestinationHandler();
@@ -145,6 +154,9 @@ class MODULES_EXPORT OfflineAudioContext final : public BaseAudioContext {
 
   // Total render sample length.
   uint32_t total_render_frames_;
+
+  // Iris: 调用方请求的采样率，用于渲染结果 AudioBuffer 的对外报告值。
+  float buffer_reported_sample_rate_ = 0;
 };
 
 }  // namespace blink
